[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-notifications"
version = "0.1.0"
description = "Event-driven tile notification system with subscriptions and handlers"
license = {text = "MIT"}
requires-python = ">=3.8"
